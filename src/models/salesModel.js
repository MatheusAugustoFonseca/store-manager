// const connection = require('./connection');

// const addNewSales = async ({ name }) => {
//   const [sales] = await connection.connection
//     .execute('INSERT INTO StoreManager.products (name) VALUES (?)', [name]);
//   // console.log(product);
//   return sales.insertId;
// };

// module.exports = {
//   addNewSales,
// };
